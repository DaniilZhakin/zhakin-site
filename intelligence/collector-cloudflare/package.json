{
  "name": "zhakin-intelligence-collector-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node worker.test.js && node schema-contract.test.js && node privacy-contract.test.js",
    "dev": "wrangler dev",
    "deploy": "npm run production:guard && wrangler deploy",
    "production:guard": "node production-config-guard.mjs",
    "d1:migrations:list": "wrangler d1 migrations list DB",
    "d1:migrations:apply:local": "wrangler d1 migrations apply DB --local"
  },
  "devDependencies": {
    "wrangler": "^4.35.0"
  }
}
